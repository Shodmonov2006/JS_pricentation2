const myName = 'Jonas'

function first() {

    if (age >= 30) {
        const decade = 3;
        var millenimal = true
    }


    function second() {
        const job = 'teacher'

        console. log('${myName} is a ${age}-ald ${job}')

    }
    second()
}